# Contributing

- Use Conventional Commits, e.g. `feat(api): add folder processing endpoint`.
- Keep PRs focused and small.
- Run linting and tests before pushing:
  - `pre-commit run --all-files`
  - `pytest -q`
- Add/update documentation when behavior changes.
